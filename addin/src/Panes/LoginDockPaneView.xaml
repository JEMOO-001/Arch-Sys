<UserControl x:Class="ArcLayoutSentinel.Panes.LoginDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="280"
             Background="{DynamicResource Esri_BackgroundPressedBrush}">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Indicator -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
            <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusColor}" Value="Green">
                                <Setter Property="Fill" Value="#4CAF50"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding StatusColor}" Value="Red">
                                <Setter Property="Fill" Value="#F44336"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding StatusColor}" Value="Orange">
                                <Setter Property="Fill" Value="#FF9800"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding StatusColor}" Value="Gray">
                                <Setter Property="Fill" Value="#9E9E9E"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="{Binding StatusText}" FontSize="11"
                       Foreground="{DynamicResource Esri_TextControlBrush}"
                       TextWrapping="Wrap" MaxWidth="230"/>
        </StackPanel>

        <!-- Username -->
        <TextBlock Grid.Row="1" Text="User Name:" Margin="0,0,0,4"
                   Foreground="{DynamicResource Esri_TextControlBrush}" FontWeight="SemiBold"/>
        <TextBox Grid.Row="2" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Padding="6,4" Margin="0,0,0,10"
                 Style="{DynamicResource Esri_TextBoxStyle}"/>

        <!-- Password -->
        <TextBlock Grid.Row="3" Text="Password:" Margin="0,0,0,4"
                   Foreground="{DynamicResource Esri_TextControlBrush}" FontWeight="SemiBold"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" Padding="6,4" Margin="0,0,0,14"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Content="Test Connection" Command="{Binding TestConnectionCommand}"
                    Padding="10,5" Margin="0,0,8,0"
                    Style="{DynamicResource Esri_ButtonStyleBorderless}"/>
            <Button Content="Log In" Command="{Binding LoginCommand}"
                    Padding="14,5" FontWeight="SemiBold"
                    Style="{DynamicResource Esri_ButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
